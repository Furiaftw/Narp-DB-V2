[build]
  command = "npm run build"
  publish = "dist"

# Vite outputs an SPA — every unknown path should fall through to index.html
# so React handles the routing instead of returning a 404.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
